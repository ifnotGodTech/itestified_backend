DJANGO_SECRET_KEY=change-me
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,10.0.2.2,192.168.1.100
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost:3000
POSTGRES_DB=itestified # 
POSTGRES_USER=itestified
POSTGRES_PASSWORD=change-me # PebD1f2tLqZfgr3R3PkUwJEERIPq4R04
POSTGRES_HOST=localhost # dpg-d84itfh9rddc739m1d9g-a
POSTGRES_PORT=5432
OTP_HINT_IN_RESPONSE=false

# Super admin bootstrap testing
SUPER_ADMIN_EMAIL=superadmin@example.com
SUPER_ADMIN_FULL_NAME=Super Admin

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-gmail-address@gmail.com
EMAIL_HOST_PASSWORD=your-google-app-password
EMAIL_USE_TLS=true
EMAIL_USE_SSL=false
DEFAULT_FROM_EMAIL=iTestified <your-gmail-address@gmail.com>
SUPPORT_EMAIL=support@itestified.app

# Mobile Google Sign-In (Phase 2)
# Comma-separated list of accepted OAuth client IDs (Android/iOS/Web as needed)
# Include the Web/Server client ID too when Android uses serverClientId.
GOOGLE_OAUTH_CLIENT_IDS=your-android-client-id.apps.googleusercontent.com,your-ios-client-id.apps.googleusercontent.com,your-web-client-id.apps.googleusercontent.com
# Optional: enforce issuer checks for Google identity tokens
GOOGLE_OAUTH_ALLOWED_ISSUERS=https://accounts.google.com,accounts.google.com

# Flutterwave donations
FLUTTERWAVE_PUBLIC_KEY=FLWPUBK_TEST-xxxxxxxxxxxxxxxxxxxxx-X
FLUTTERWAVE_SECRET_KEY=FLWSECK_TEST-xxxxxxxxxxxxxxxxxxxxx-X
FLUTTERWAVE_SECRET_HASH=
FLUTTERWAVE_BASE_URL=https://api.flutterwave.com
FLUTTERWAVE_REDIRECT_URL=https://your-mobile-or-web-return-url

# to bootstrap the super admin, run the following command after setting the environment variables:

./.venv/bin/python backend/manage.py bootstrap_super_admin \
  --email "your-email@example.com" \
  --full-name "Your Name"


On render shell
python manage.py bootstrap_super_admin \
  --email "itestified2022@gmail.com" \
  --full-name "iTestified Admin"

#   Super admin bootstrap completed.
# email=itestified2022@gmail.com
# temporary_password=GbGjW#umKDxUZB9wWjhJ

pip install -r requirements/production.txt && python manage.py collectstatic --noinput && python manage.py migrate --noinput

# Deploy.
# Open Render deploy logs and copy the printed temporary password.
# Log in to dashboard/admin and change password immediately.
# Remove the bootstrap line from Build Command right after, then redeploy.

pip install -r requirements/production.txt && python3 manage.py collectstatic --noinput && python3 manage.py migrate --noinput && python3 manage.py bootstrap_super_admin --email "itestified2022@gmail.com" --full-name "iTestified Admin"
